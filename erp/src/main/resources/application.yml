spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myerp?createDatabaseIfNotExist=true
    username: root
    password: "@D3V_147wc258"


  jpa:
    show-sql: true

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}